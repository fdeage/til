# J10 - Checking multiple checkboxes with shift

When an event is fired, alternative keys are checked with booleans:
`altKey`, `shiftKey`, `ctrlKey`, `metaKey`, `shiftKey`
If one of them is pressed, the boolean is true.
